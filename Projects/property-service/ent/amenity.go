// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/evolve-interviews/paxtonedgar-case-study/ent/amenity"
	"github.com/google/uuid"
)

// Amenity is the model entity for the Amenity schema.
type Amenity struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Unique amenity code (e.g., 'PARKING', 'HOT_TUB', 'WIFI')
	Code string `json:"code,omitempty"`
	// Human-readable amenity name (e.g., 'Parking', 'Hot Tub', 'Wi-Fi')
	Name string `json:"name,omitempty"`
	// Amenity category (e.g., 'Parking', 'Wellness', 'Connectivity')
	Category string `json:"category,omitempty"`
	// When the amenity was created
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the AmenityQuery when eager-loading is set.
	Edges        AmenityEdges `json:"edges"`
	selectValues sql.SelectValues
}

// AmenityEdges holds the relations/edges for other nodes in the graph.
type AmenityEdges struct {
	// Properties that have this amenity
	Properties []*Property `json:"properties,omitempty"`
	// PropertyAmenities holds the value of the property_amenities edge.
	PropertyAmenities []*PropertyAmenity `json:"property_amenities,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// PropertiesOrErr returns the Properties value or an error if the edge
// was not loaded in eager-loading.
func (e AmenityEdges) PropertiesOrErr() ([]*Property, error) {
	if e.loadedTypes[0] {
		return e.Properties, nil
	}
	return nil, &NotLoadedError{edge: "properties"}
}

// PropertyAmenitiesOrErr returns the PropertyAmenities value or an error if the edge
// was not loaded in eager-loading.
func (e AmenityEdges) PropertyAmenitiesOrErr() ([]*PropertyAmenity, error) {
	if e.loadedTypes[1] {
		return e.PropertyAmenities, nil
	}
	return nil, &NotLoadedError{edge: "property_amenities"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Amenity) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case amenity.FieldCode, amenity.FieldName, amenity.FieldCategory:
			values[i] = new(sql.NullString)
		case amenity.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case amenity.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Amenity fields.
func (_m *Amenity) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case amenity.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case amenity.FieldCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field code", values[i])
			} else if value.Valid {
				_m.Code = value.String
			}
		case amenity.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case amenity.FieldCategory:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field category", values[i])
			} else if value.Valid {
				_m.Category = value.String
			}
		case amenity.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Amenity.
// This includes values selected through modifiers, order, etc.
func (_m *Amenity) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryProperties queries the "properties" edge of the Amenity entity.
func (_m *Amenity) QueryProperties() *PropertyQuery {
	return NewAmenityClient(_m.config).QueryProperties(_m)
}

// QueryPropertyAmenities queries the "property_amenities" edge of the Amenity entity.
func (_m *Amenity) QueryPropertyAmenities() *PropertyAmenityQuery {
	return NewAmenityClient(_m.config).QueryPropertyAmenities(_m)
}

// Update returns a builder for updating this Amenity.
// Note that you need to call Amenity.Unwrap() before calling this method if this Amenity
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Amenity) Update() *AmenityUpdateOne {
	return NewAmenityClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Amenity entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Amenity) Unwrap() *Amenity {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Amenity is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Amenity) String() string {
	var builder strings.Builder
	builder.WriteString("Amenity(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("code=")
	builder.WriteString(_m.Code)
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("category=")
	builder.WriteString(_m.Category)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Amenities is a parsable slice of Amenity.
type Amenities []*Amenity
